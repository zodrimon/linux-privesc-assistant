# default_config.yaml

output:
  format: terminal
  file: ""
  verbose: false

timeout_per_check: 30

checks:
  suid_sgid: true
  capabilities: true
  writable_path: true
  cron_jobs: true
  weak_permissions: true
  kernel_cve: true
  misconfigurations: true
